/vendor/
/node_modules/
# /admin-app/build/ — committed until CI pipeline is added
# Plugin release staging output (bin/build-release.py writes here)
/build/
.phpunit.result.cache
*.log
*.zip
.env
# Internal plans/specs/audits live in the PRIVATE repo
# MaxHandMade/mhm-currency-switcher-docs (cloned at c:/projects/mhm-currency-switcher-docs).
# This repo is public and WP.org-facing — internal design docs must never land here.
/docs/superpowers/
.DS_Store
Thumbs.db
composer.lock
